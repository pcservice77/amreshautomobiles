rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Unrestricted access for all core showroom operations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}